export interface Testimonial {
  name: string;
  rating: number;
  quote: string;
  avatar?: string;
}

export interface ScienceSection {
  title: string;
  symbol: string;
  mainText: string;
  howItWorks: string;
  studies: string;
}
